name: provision-infrastructure

on:
    workflow_dispatch:
        inputs:
            azureClientId:
                description: Azure federated identity client id
                required: true
                type: string
            azureTenantId:
                description: Azure tenant id
                required: true
                type: string
            azureSubscriptionId:
                description: Azure subscription id
                required: true
                type: string
            azureLocation:
                description: Azure location
                required: true
                default: eastus2
                type: string
            azdEnvironmentName:
                description: azd environment name
                required: true
                default: dev
                type: string

jobs:
    provision:
        runs-on: ubuntu-latest
        permissions:
            id-token: write
            contents: read
        steps:
            - uses: actions/checkout@v6
            - uses: ./.github/actions/azure-login-and-azd-setup
              with:
                  clientId: ${{ inputs.azureClientId }}
                  tenantId: ${{ inputs.azureTenantId }}
                  subscriptionId: ${{ inputs.azureSubscriptionId }}
                  environmentName: ${{ inputs.azdEnvironmentName }}
                  location: ${{ inputs.azureLocation }}
            - name: Provision infra
              run: azd provision --cwd . --environment "${{ inputs.azdEnvironmentName }}"
